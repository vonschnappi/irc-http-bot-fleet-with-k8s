apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-joe
  labels:
    app: bot-joe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot-joe
  template:
    metadata:
      labels:
        app: bot-joe
    spec:
      containers:
      - name: bot-joe
        imagePullPolicy: Never
        image: irc-http
        ports:
        - containerPort: 3000
        env:
        - name: IRC_HOST
          value: chat.freenode.net
        - name: IRC_PORT
          value: "6667"
        - name: IRC_NICK
          value: bot-joe
        - name: IRC_CHANNEL
          value: hackeriet  
        - name: DEBUG
          value: "1" 
---
apiVersion: v1
kind: Service
metadata:
  name: bot-joe-service
spec:
  selector:
    app: bot-joe
  type: LoadBalancer  
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30803
