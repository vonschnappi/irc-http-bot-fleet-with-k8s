apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-jennifer
  labels:
    app: bot-jennifer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot-jennifer
  template:
    metadata:
      labels:
        app: bot-jennifer
    spec:
      containers:
      - name: bot-jennifer
        imagePullPolicy: Never
        image: irc-http
        ports:
        - containerPort: 3000
        env:
        - name: IRC_HOST
          value: chat.freenode.net
        - name: IRC_PORT
          value: "6667"
        - name: IRC_NICK
          value: bot-jennifer
        - name: IRC_CHANNEL
          value: hackeriet  
        - name: DEBUG
          value: "1" 
---
apiVersion: v1
kind: Service
metadata:
  name: bot-jennifer-service
spec:
  selector:
    app: bot-jennifer
  type: LoadBalancer  
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32334
